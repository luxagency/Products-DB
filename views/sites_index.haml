%h1 Listing sites

- unless @sites.empty?
  %table
    %tr
      %th Name
      %th url
      %th clicks
      %th by category
      %th by tags
      %th actions
    - @sites.each do |site|
      %tr
        %td=site.name
        %td=site.url
        %td=site.clicks.to_i
        %td=site.clicks_per_category
        %td=site.clicks_per_tag
        %td= link_to "Edit", "/sites/#{site.id}/edit"
- else
  No sites found

%br
%p= link_to "New Site", "/sites/new"